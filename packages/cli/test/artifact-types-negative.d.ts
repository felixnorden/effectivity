// The generated-artifact shapes moved to @effectivity/cloudflare; the engine
// entry must not expose them again. If this import ever stops erroring, the
// @ts-expect-error becomes unused and the type-check task fails.
// @ts-expect-error RuntimeSettings is owned by @effectivity/cloudflare
import type { RuntimeSettings } from "@effectivity/cli"

declare const engineSettings: RuntimeSettings
export { engineSettings }
