{
  "name": "@effectivity/examples",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "fmt": "oxfmt",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.config.json",
    "test": "vitest run && vitest run --config vitest.config.node.ts",
    "prepare": "effect-tsgo patch --typescript --oxlint",
    "cms": "effectivity",
    "seed": "bash ./scripts/seed.sh",
    "dev": "effectivity dev",
    "build": "effectivity build",
    "preview": "effectivity preview",
    "sync": "effectivity sync"
  },
  "dependencies": {
    "@effectivity/cli": "workspace:*",
    "@effectivity/cloudflare": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "catalog:",
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@cloudflare/workers-types": "catalog:",
    "@effect/tsgo": "catalog:",
    "@effect/vitest": "catalog:",
    "@effectivity/auth": "workspace:*",
    "@effectivity/core": "workspace:*",
    "@types/bun": "catalog:",
    "effect": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
